# src/

This directory is reserved for reusable helper modules supporting the deterministic open-ended RSI demo.

Current implementation keeps logic in `run_demo.py` to maximize reviewability for RC hardening.
